{
  "//": "ywr-harness declaration. Repo-specific values live HERE so the plugin's scripts stay identical in every repo (ADR 0010). Placed once by /harness-init and never overwritten — edit it freely. Every command-shaped field is a SELECTOR from a closed set defined in the plugin, never a command string: this file is in the working tree, and the scripts print commands that get run (ADR 0012). A gate that is this repo's own script is declared as a script gate — closed-set runner + validated repo-relative path, still never a command string (ADR 0024).",

  "docs": {
    "index": "docs/index.json",

    "//site_title": "The <title>/header of the generated docs surfaces (docs.html · docs.artifact.html; the customer surface derives its default from it). THIS declaration is the source — the DOCS_SITE_TITLE env var is a one-off override only, so a harness-init re-run or CI rebuild can never revert a committed title (ADR 0050). Convention: '<repo> · <purpose>', same as artifact titles (ADR 0032). Empty = the builder's default. Display text only — it is escaped into HTML and never reaches a command.",
    "site_title": "",

    "//customer": "OPTIONAL customer-corpus surface (ADR 0060). null = off (the default build stays 4 surfaces). An object turns it on: every subdirectory of 'dir' (default docs/customer) is one program — its name is the program id — and MUST hold spec.md (the record: frontmatter title · updated · program == the directory name), user-guide.md, and release-notes.md (one '## YYYY-MM-DD — title [TAG]' per entry, newest first); the builder renders them into ONE docs/customer.artifact.html and REFUSES the build (exit 1) when the directory is missing or a program lacks a file or a required key. Keys — display text (escaped into HTML, never a command): title, eyebrow, description, contact, other_label, unkeyed_label, chip_suffix (appended to the chip value ONLY in PROJECTS.md's file-form listing, e.g. '.p' → `xgglbsrp.p`; the HTML chip shows the bare value). Frontmatter key names: chip_field (rendered as each program's mono chip; required on spec.md when declared), required_frontmatter (extra keys spec.md must carry), group_field (default 'menu'; the LEADING INTEGER of its first comma-separated value picks the sidebar group), project_field (default 'project'). groups: [{\"label\": \"재무회계 (25.x)\", \"keys\": [25]}, …] in display order, first match wins; a program whose key matches none lands under other_label, one without a parseable key under unkeyed_label. projects_md: true also writes docs/PROJECTS.md (project → programs; COMMITTED like INDEX.md). release_stages: [{\"tag\": \"FUT\", \"label\": \"FUT\"}, …] — the [TAG] vocabulary release entries may carry, in stage order, the LAST being the final stage (default FUT · UAT · PROD→COMPLETE). panels: [{\"module\": \"scripts/ops/customer_env_panel.py\", \"label\": \"환경 차이\", \"id\": \"env\", \"position\": \"after-docs\"}, …] — EXTENSION POINT: each module is a repo-relative .py path (same allowlist as a script-gate path — the builder IMPORTS it) exposing `render_panel(repo_root) -> str` (an HTML fragment) and optionally `CSS` (a str), `UPDATED` (a 'YYYY-MM-DD' str folded into the footer date — NO clock) and `TAB_CHIPS` (a list of str rendered as chips inside the tab button); each becomes one more top-level tab. id (ADR 0061): optional ASCII token, the tab's data-top / #top-<id> deep link / panel-<id> scope class — declare it so links survive reordering (absent → label slug, else panel-N). position: before-docs | after-docs (default) — order is before-docs panels, the documents tab, after-docs panels. default_tab: 'docs' or a panel id — the tab the page opens on (default: the first in order). footer_date_label: the footer wording before the date (default 문서 생성일). Panel CSS is appended to ONE global <style>: SCOPE every selector under .panel-<id> (an unscoped `.mono{}` restyles the documents tab; the builder warns when a selector reuses a class the canon or another panel styles without any panel-owned class in it). 'dir' is held to the path allowlist ([A-Za-z0-9._/-], no '..'): a traversal would publish files from outside the repo into a customer page. Not a whitelist surface: docs/customer.html (--customer, ADR 0005) is a different, frontmatter-selected page and keeps working unchanged.",
    "customer": null
  },

  "//handoff": "Resume-point doc the slice close updates (reporting only — never reaches a command). A single file serves a single active work line; a value ending in '/' declares a DIRECTORY holding one resume file per work line (multi-writer repos), each named after the work line, never after a person (ADR 0040). Either form, the handoff is a resume point, not a ledger: the close that supersedes a Current state section moves it to docs/handoff-archive/<same filename>.",
  "handoff": "",

  "//artifacts": "Declared claude.ai Artifacts (ADR 0032). When this repo publishes an Artifact (onboarding guide, docs page from docs/docs.artifact.html, …), declare it here: the gate emitter checks that 'readme' carries each declared url and each declared title starts with the repo name (naming convention: <repo> · <purpose>), and CI fails on a violation. The gate cannot see claude.ai, so an undeclared Artifact is invisible to it — the emitter reports the check as disabled until something is declared. A GENERATED Artifact whose source file is committed additionally declares 'source' (repo-relative path of that file — must exist, allowlist-validated) and 'check' ({runner, script} — the script-gate shape, ADR 0024; always whole-program; the script's default mode must be a drift check that exits 1 on drift and writes nothing, the build_onboarding.py contract): the emitter emits the check under gates: so CI runs it and pre-commit defers it, and /ywr-harness:artifact-publish verifies then publishes with one confirmation (ADR 0068).",
  "artifacts": {
    "readme": "README.md",
    "items": []
  },

  "review": {
    "//": "Regexes. They select and report — they cannot name an executable, so free strings are safe here.",
    "canon": "REVIEW.md",

    "//docs_only": "Surfaces that carry no executable meaning. When EVERY changed file matches, the review tier is 'skip' and deterministic gates suffice.",
    "docs_only": ["^docs/", "^[^/]*\\.md$"],

    "//harness_layer": "Code-bearing surfaces that are tooling rather than product. When every code-bearing file is confined here, the tier is 'small'.",
    "harness_layer": [],

    "//critical": "Surfaces where a weaker review is worst. ANY match forces tier 'full' — size never overrides criticality. Typical: migrations, authn/authz, tenant isolation, a deterministic numeric core, CI and guard semantics. The declaration file itself needs no entry: its criticality is computed from which keys changed (ADR 0038 — metadata keys docs/handoff/artifacts and comments never force it; every other key always does).",
    "critical": [],

    "//derived": "Byte-identical placements of a canonical source — generated or vendored copies a deterministic gate holds identical (ADR 0037). A changed copy whose CURRENT bytes equal its mapped source's stops counting toward the size measure and cannot force 'critical'; a drifted copy counts fully. Path PREFIXES ending in '/' ([A-Za-z0-9._/-] only), each copy mapping directly to its canonical source — chains are not resolved. Empty disables the exemption.",
    "derived": []
  },

  "retro": {
    "//": "Slice retro gate (ADR 0017). Regexes, like 'review' — they select and report, so free strings are safe. An EMPTY list DISABLES the checks it drives; the disablement is reported by `harness_retro.py --coverage` rather than passing quietly.",

    "//source_scope": "Product source that must be owned by a spec's implements_in. Drives UNMAPPED, which fires on NEWLY ADDED files only — new code is owned from day one and the legacy baseline does not spam every commit.",
    "source_scope": [],

    "//dep_manifests": "Dependency manifests. Drives DEP: changed with no new ADR in scope. List the manifest, NOT the lockfile — a lockfile bump is a version change, not a decision.",
    "dep_manifests": [],

    "//migrations": "Schema migration files. Drives MIGRATION: added with no living spec touched.",
    "migrations": [],

    "//ignore_file": "The UNMAPPED exemption list, which doubles as the visible spec-debt register.",
    "ignore_file": ".githooks/slice-retro-ignore"
  },

  "groups": [
    {
      "//": "One entry per file family that has its own gates. 'match' is a regex anchored at the repo root; 'cwd' is where the command runs; 'strip_prefix' is removed from file paths before they are passed. 'gates' are SELECTORS: ruff | ruff-format | pytest | pytest-nondb | eslint | prettier | tsc | vitest | cargo-clippy | cargo-test | go-vet | go-test | actionlint. A gate outside the set is dropped with the set named. A gate that is this REPO'S OWN SCRIPT (a test entry point no repo-independent selector could name) is a script-gate object: {\"runner\": \"pwsh\", \"script\": \"tools/selftest.ps1\", \"files\": false} — runner is a selector from the verify.runner closed set, script is a repo-relative path held to an allowlist ([A-Za-z0-9._/-] only, no '..', no leading '/' or '-'; refused otherwise), files:true appends the slice's changed files. There is deliberately no argument field: a gate that needs flags wraps them inside the script (ADR 0024). Need a shared TOOL that is missing? Add it to the plugin's closed set, where it is reviewed once and reaches every repo. Paths the scaffold itself places (scripts/harness/, .githooks/, the CI workflow, the seeds, .harness-version) are claimed BUILT-IN by the emitter and need no entry here (ADR 0044) — declare groups only for files this repo authors.",
      "name": "example-python",
      "match": "^src/.*\\.py$",
      "cwd": "",
      "strip_prefix": "",
      "gates": []
    },
    {
      "//": "The Claude Code host's own project settings. Claimed because `claude plugin install --scope project` writes .claude/settings.json into the tree, and an unclaimed file fails CI (and reads as missing coverage locally). Claiming it is NOT permission to commit `enabledPlugins`: that key forces the plugin on every clone, and CI and pre-commit refuse it in both settings files regardless of its value (ADR 0010, ADR 0022).",
      "name": "claude-host",
      "match": "^\\.claude/settings(\\.local)?\\.json$",
      "cwd": "",
      "strip_prefix": "",
      "gates": []
    },
    {
      "//": "The decision records this repo accumulates (docs/adr append-only, docs/spec living). Claimed so the first ADR you write — the whole point of the shape — does not fail the partition audit (ADR 0052). Prose has no deterministic gate; the docs build is the pipeline that consumes these.",
      "name": "docs-corpus",
      "match": "^docs/(adr|spec)/.*\\.md$",
      "cwd": "",
      "strip_prefix": "",
      "gates": []
    },
    {
      "//": "The builder outputs the pipeline COMMITS (index.json + INDEX.md — the html surfaces are gitignored, ADR 0053; PROJECTS.md is committed like INDEX.md when docs.customer.projects_md is declared true, ADR 0060). If this repo moves docs.index, update this match with it (ADR 0052).",
      "name": "docs-generated",
      "match": "^docs/(index\\.json|INDEX\\.md|PROJECTS\\.md)$",
      "cwd": "",
      "strip_prefix": "",
      "gates": []
    },
    {
      "//": "Root prose every repo has. Widen this as the repo's own prose accumulates (CONTRIBUTING.md, a handoff doc, …) — a new prose file surfacing as ungrouped is this group asking to be widened (ADR 0052).",
      "name": "prose",
      "match": "^README\\.md$",
      "cwd": "",
      "strip_prefix": "",
      "gates": []
    }
  ],

  "verify": {
    "//runner": "SELECTOR: python-uv | python | poetry | node | pwsh.",
    "runner": "python",
    "cwd": "",
    "strip_prefix": "",

    "//script_pattern": "Regex identifying a verify script inside a spec's implements_in.",
    "script_pattern": "^.*/verify_.*\\.py$",

    "//product_scope": "Regex for files that SHOULD have a spec owner. Matches with no owner are reported as unmapped. Empty disables the check.",
    "product_scope": "",

    "//ui_prefix": "Path prefix whose changes the registered scripts do not cover. Empty disables the note.",
    "ui_prefix": ""
  }
}
