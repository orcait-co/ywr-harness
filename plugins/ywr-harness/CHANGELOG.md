# ywr-harness 릴리스 노트

> 멤버용 변경 이력입니다 — 새 버전이 처음 로드된 세션이 시작될 때, 세션 시작 훅이 이 파일의
> 해당 버전 항목을 요약해 한 번 안내합니다 (ADR 0030). 전체 이력은 온보딩 가이드의 릴리스 노트
> 탭에서도 볼 수 있습니다 (claude.ai **Team 좌석 로그인 필요**):
>
> https://claude.ai/code/artifact/a4387fdf-63d1-4a3d-9c8e-c362c9215a54#rn
>
> 형식 계약: 항목은 최신순, 제목은 `## vX.Y.Z — YYYY-MM-DD`, 본문은 `- ` 불릿. 이 중 게이트
> (`manifest-gate.ps1`)가 강제하는 것은 두 가지뿐입니다 — 맨 위 항목의 버전이 `plugin.json`과
> 일치할 것, 위 링크가 안내 훅이 인쇄하는 링크와 일치할 것. 정렬·날짜·불릿 형식은 검사되지
> 않는 컨벤션이며, 깨지면 세션 시작 안내가 불릿 없는 형태로 조용히 저하됩니다.

## v0.38.1 — 2026-08-27

- **셀프테스트 수정 1건** (동작 변경 없음): v0.38.0 의 빌더 셀프체크(`_selfcheck_release_helpers`,
  빌드에서는 호출되지 않고 셀프테스트만 부르는 함수)가 픽스처에 없는 `[FUT]` 배지를 단정해
  CI(Python 3.12)에서 실패했습니다. 로컬(3.13)에서는 `-c` 트레이스백이 명령 소스를 되비추는
  탓에 센티널이 자기 자신에 매칭돼 통과로 읽혔습니다 — 단정을 픽스처의 UAT/PROD 로 고치고,
  셀프테스트는 종료 코드와 트레이스백 부재까지 함께 봅니다. 생성되는 문서 표면은 0.38.0 과
  바이트 동일하고, 멤버 조치는 없습니다.

## v0.38.0 — 2026-08-27

- **고객 문서 코퍼스 표면이 선언으로 켜집니다** (ADR 0060): `.harness.json` `docs.customer` 를
  채우면 `docs/customer/<프로그램>/{spec,user-guide,release-notes}.md` 코퍼스가 하나의
  `docs/customer.artifact.html`(+ 선택 `docs/PROJECTS.md`)로 렌더링됩니다 — 사이드바 그룹·릴리즈
  배지·프로그램 칩·확장 탭(`panels[]`)이 전부 선언 데이터입니다. 기본값 `null` 이면 아무것도
  달라지지 않습니다. **client-pjems 조치, 이 순서로**: ① `/ywr-harness:harness-init` 재실행(새
  빌더와 새 리더가 함께 배치됩니다 — 포크한 `docs/build_docs.py` 는 이때 덮어써도 됩니다) →
  ② `.harness.json` `docs.customer` 를 채움 → ③ `pwsh docs/build.ps1`. 순서가 어긋나 리더가
  구버전인 채로 선언만 있으면 빌드는 트레이스백 대신 "리더가 구버전" 이라고 지목하며 exit 1
  로 멈춥니다. 환경 차이 탭은 렌더러를 `panels[]` 모듈로 옮겨야 유지됩니다.
- **마크다운 렌더링 수정 3종** (모든 표면 공통): 하드랩된 리스트 항목의 연속 줄이 같은 항목으로
  합쳐지고, ```` ```diff ```` 펜스가 +/− 줄 채색되고, 표가 가로 스크롤 래퍼로 감싸집니다.
  재빌드하면 `docs.html` 마크업이 조금 달라지지만 커밋 대상(index.json·INDEX.md)은 그대로입니다.
- **선언 검증 강화**: `docs.customer` 의 잘못된 값은 항목별로 경고되고 항목만 버려지며, 빈
  문자열도 "미선언"이 아니라 경고 후 기본값입니다. 멤버 조치 없음 — 기존 레포는 다음 init
  재실행 때 `.harness.json` 이 `docs.customer` 키를 갖지 않는다는 드리프트 노트 한 줄을 봅니다
  (`"customer": null` 을 추가하면 조용해집니다).

## v0.37.0 — 2026-08-19

- **상태줄에 worktree 세그먼트가 추가됩니다** (ADR 0059): linked git worktree 안에서 세션이
  돌 때 위치 옆에 `wt <이름>` 이 표시됩니다 — 백그라운드 에이전트 시대에 "지금 어느
  체크아웃인가"를 한눈에. 메인 워킹트리에서는 아무것도 달라지지 않습니다.
- **상태줄이 유휴 중에도 30초마다 갱신됩니다** (ADR 0059): 설치 스크립트가 `statusLine`
  블록에 `refreshInterval: 30` 을 씁니다. 이미 설치된 블록에는 키가 없을 때만 추가하고,
  직접 조정한 값은 어떤 값이든 보존합니다. **조치**: 기존 설치 머신은
  `statusline/install.ps1` 재실행 한 번으로 두 변경을 모두 받습니다 (배치 사본은 인스톨러
  재실행 때만 갱신됩니다 — 플러그인 업데이트만으로는 반영되지 않습니다).
- **조직 관리 설정이 워크플로 크기 가이드라인을 배포합니다** (ADR 0058, 플러그인이 아니라
  관리 설정 경로로 전달): `workflowSizeGuideline: "medium"` — 조직 가이드의 fan-out 비례
  원칙이 정책으로 고정됩니다. advisory 라 강제 상한이 아니고 프롬프트로 더 큰 규모를 요청할
  수 있지만, 배포 중에는 `/config` 의 "Dynamic workflow size" 행이 숨겨집니다. 멤버 조치는
  없습니다 (관리 설정은 자동 전파).

## v0.36.0 — 2026-08-17

- **신규 스캐폴드 레포의 첫 push CI가 이제 그린입니다** (이슈 #47, ADR 0052): 템플릿
  `.harness.json`이 정본이 손으로 선언하던 세 파티션 그룹(`docs-corpus`·`docs-generated`·
  `prose`)을 기본 동봉해, 스캐폴드 자신의 산출물과 여러분이 쓰는 ADR이 더 이상 ungrouped 로
  CI를 붉히지 않습니다. 시드라 기존 레포에는 닿지 않습니다 — 이미 그룹을 선언해 그린인
  레포는 그대로 두면 됩니다.
- **루트 `.gitignore` 시드** (이슈 #48, 감사 M3, ADR 0053): 재생성 가능한 html 산출물
  (`docs/docs.html` 등), 텔레메트리 ledger(`.claude/telemetry/`), `.env`, 파이썬 바이트코드가
  첫 커밋에 들어가지 않습니다. 기존 `.gitignore`는 보존되고 부족한 라인은 재실행 시 드리프트
  노트로 보고됩니다. **조치**: 기존 스캐폴드 레포는 harness-init 재실행 한 번으로 시드를
  받습니다 (없던 시드는 재실행 때 새로 배치됩니다).
- **시작용 `REVIEW.md` 시드** (이슈 #49, ADR 0054): 모든 신규 레포의 첫 slice-close 를 막던
  `review canon: NOT FOUND` 하드스톱이 사라집니다. 배치되는 것은 형식 + 공통 불변식 2개의
  스타터입니다 — **이 레포의 불변식으로 교체하세요** (After-running 4단계로 추가됨).
  templates/CLAUDE.md 의 오독 문구("shipped by the plugin")도 교정.
- **브라운필드 첫 실행이 기존 파일을 더 이상 조용히 교체하지 않습니다** (이슈 #50, ADR 0055):
  `.harness-version` 스탬프가 없는 첫 실행에서 TOOLCHAIN 경로에 내용이 다른 기존 파일이 있으면
  REFUSE 하고 병합/이동 후 재실행을 안내합니다. 의도적 교체는 `-Force` (라벨도 `REPLACED` 로
  정직하게 표기). 충돌이 해소될 때까지 스탬프를 쓰지 않아 다음 실행이 덮어쓰기 시맨틱으로
  넘어가지 않습니다. 기존 docs/ 가 있는 레포는 `-DryRun` 먼저.
- **Windows PowerShell 5.1 에서 명확히 거절합니다** (이슈 #51): `init.ps1`·`selftest.ps1`·
  `statusline/install.ps1` 에 `#Requires -Version 7.0` — 이전에는 무의미한 null 메서드 에러로
  죽었습니다. pwsh 7 은 원래 선행조건입니다 (README §Prerequisites).
- **훅 실행비트 갭을 emitter 가 보고합니다** (이슈 #53, ADR 0056): Windows 에서 스캐폴드한
  레포의 훅은 인덱스 모드 100644 라 POSIX/WSL 클론에서 git 이 조용히 건너뜁니다. 이제
  `hooks:` 라인이 POSIX(CI ubuntu 포함)에서 실행비트 결손을 치유 커맨드와 함께 보고합니다
  (`chmod +x .githooks/*` + `git update-index --chmod=+x .githooks/*`).
- **출력 문구 정정** (이슈 #52): 스탬프 라인의 "claim it in .harness.json groups" 가 ADR 0044
  의 built-in 청구와 모순이라 "claimed built-in by the emitter" 로 교체.

## v0.35.0 — 2026-08-12

- **docs 사이트 타이틀이 더 이상 리버트되지 않습니다** (이슈 #43, ADR 0050): 타이틀의 정식
  소스가 `.harness.json`의 새 필드 `docs.site_title` 로 이동했습니다. 이전에는
  `DOCS_SITE_TITLE` 환경변수뿐이라 harness-init 재실행·CI 재빌드가 커밋된
  `docs.html`/`docs.artifact.html`의 `<title>`을 기본값으로 되돌렸습니다. env 는 1회성
  오버라이드로 남습니다. **조치**: 타이틀을 쓰는 레포는 `.harness.json` `docs` 섹션에
  `"site_title": "<레포명> · <용도>"` 한 줄을 추가하세요 (`.harness.json`은 시드라 자동
  갱신되지 않습니다). 한글 타이틀을 env 로 넘기던 우회(인코딩 깨짐)도 함께 불필요해집니다.
- **harness-init 재실행이 보존 시드의 템플릿 드리프트를 알려줍니다** (이슈 #44, ADR 0051):
  보존된 `.harness.json`/`.gitattributes`/`slice-retro-ignore`가 현재 템플릿에는 있는
  키/라인을 결여하면, preserved 리포트 라인에 무엇이 없는지 한 줄로 표시합니다 — 예:
  `template has 1 key(s) this seed lacks: review.derived`. 읽기 전용 안내이며 아무것도
  덮어쓰지 않습니다. `CLAUDE.md`는 전면 재작성이 정상이라 대상에서 제외.
- **출력 문구 2건 정정** (이슈 #45): init 빌드 요약이 네 번째 산출물 `docs.artifact.html`을
  포함해 표기합니다. pre-commit 훅은 "매칭된 그룹이 `gates: []`"인 경우와 "어느 그룹에도
  미매칭"(커버리지 구멍)을 구분해 보고합니다 — 전자는 그룹 이름을 지목합니다.

- **상태바의 Fable 인라인 주간 표시(`Fable 5(7d N%)`)가 제거되었습니다** (ADR 0049 — 0047
  supersede): 그 숫자는 Fable 전용이 아니라 **계정 전체** 주간 사용량이라, 모델 이름 옆이라는
  위치 때문에 Fable 수치로 오독되었습니다. `/usage`에는 Team 플랜 Fable 캡(주간 한도의 50%)
  대비로 계산되는 **진짜 Fable 전용 바**가 따로 있어 두 숫자가 다르게 보입니다(예: 전체 13%
  vs Fable 24% — 분모가 다를 뿐 둘 다 맞는 값). 라이브 페이로드 재측정(2.1.228) 결과 상태바
  페이로드에는 모델별 수치가 없어 정직하게 표시할 방법이 없고, 주간 수치는 모든 모델과
  동일하게 꼬리 `7d` 칸으로 돌아갑니다. 모델별 키 추가를 업스트림에 요청했습니다
  (anthropics/claude-code#85964) — 키가 생기면 진짜 Fable 수치로 인라인을 복원합니다.
- **상태바 설치본은 자동 갱신되지 않습니다**: 플러그인 업데이트만으로는 반영되지 않으니,
  세션에서 `statusline/install.ps1` 실행을 요청해 설치본을 갱신하세요 (spec 0010).

## v0.33.1 — 2026-08-12

- **한글 등 비ASCII 파일명이 있는 레포의 CI 실패가 고쳐졌습니다** (이슈 #40, orcait-co/aws
  main 푸시 6연속 실패로 발견): git 기본 설정(`core.quotepath=true`)이 비ASCII 경로를
  8진 이스케이프(`"docs/AWS \354..."`)로 출력해 그룹 패턴이 불일치 → 해당 파일 전부가
  미클레임(ungrouped)으로 오판되어 full-tree 감사가 실패하던 버그입니다. 로컬 클론은 대개
  `quotepath=false`가 설정돼 있어 CI에서만 발화했습니다. 함께 고침: Windows 한국어
  콘솔(cp949)에서 경로 디코드가 깨질 수 있던 잠재 크래시(명시적 UTF-8 디코드 핀), 그리고
  retro 스크립트의 동일 클래스 결함.
- **스캐폴드 레포 조치 1건**: 수정은 레포에 배치된 `scripts/harness/` 사본에 있으므로,
  플러그인 업데이트(`claude plugin update ywr-harness@ywrlabs`) 후 `harness-init` 리프레시를
  한 번 실행해야 CI에 반영됩니다. orcait-co/aws처럼 로컬 선반영(#136)한 레포는 리프레시가
  선반영분을 canon과 같은 내용으로 덮어씁니다 — 되돌림 걱정 없음.

## v0.33.0 — 2026-08-11

- **온보딩 가이드·릴리스 노트 링크가 새 주소로 바뀌었습니다** (ADR 0048):
  https://claude.ai/code/artifact/a4387fdf-63d1-4a3d-9c8e-c362c9215a54 — 세션 시작 안내와 이
  파일 상단의 링크가 모두 새 주소를 가리킵니다. **북마크를 갱신하세요**: 이전 주소는 더 이상
  갱신되지 않으며 곧 이동 안내 페이지로 교체됩니다 (내용은 새 주소에 그대로,
  v0.31.0·v0.32.0 노트 포함). 링크가 안 열리면 Team 좌석 로그인 확인 후 John에게 공유를
  요청하세요.

## v0.32.0 — 2026-08-11

- **Fable 세션은 주간 사용량이 모델 옆에 붙습니다** (ADR 0047): 모델이 Fable이면 상태바가
  `Fable 5(7d 30%)`처럼 주간 수치를 모델 칸 안에 표시하고, 꼬리의 `7d` 칸은 사라집니다 —
  같은 숫자가 자리만 옮기는 것이라 한 줄에 두 번 나오지 않습니다. 이 수치는 Claude Code가
  상태바에 주는 유일한 주간 값(모든 모델 합산)입니다 — Fable 전용 주간은 페이로드에 없어서,
  값이 없으면 아무것도 표시하지 않습니다(0%로 꾸며내지 않음). Fable이 아닌 모델은 이전과
  동일합니다. 색 기준(50% 주의 · 80% 경고)도 기존 `7d` 칸 그대로입니다.

## v0.31.0 — 2026-08-11

- **상태줄이 멀티계정을 이해합니다** (ADR 0046): `CLAUDE_CONFIG_DIR`로 계정을 나눠 쓰는 머신에서
  ① 터미널 탭 타이틀이 폴더 대신 **계정**(`claude-ywrlabs` 같은 config 디렉터리 이름)을
  표시합니다 — 폴더는 상태줄 첫 세그먼트에 이미 있으므로, 탭은 상태줄이 못 보여주는 유일한
  차원을 보여줍니다. ② 상태줄 끝의 플러그인 버전이 **지금 실행 중인 계정**의 설치 레지스트리를
  읽습니다 (이전에는 `~/.claude` 고정이라 다른 계정의 버전이 표시될 수 있었습니다).
  ③ `statusline/install.ps1`을 플래그 없이 실행하면 세션의 `CLAUDE_CONFIG_DIR`에 설치됩니다
  (이전에는 조용히 `~/.claude`로 갔습니다). `CLAUDE_CONFIG_DIR`을 쓰지 않는 머신은 세 가지
  모두 이전과 동일하게 동작합니다 — 조치 없음.
- **멀티계정 사용자 조치 1건**: 배치된 상태줄 사본은 플러그인 업데이트로는 갱신되지 않으므로
  (TOOLCHAIN 규칙), 계정별로 한 번씩 `statusline/install.ps1`을 재실행하세요.

## v0.30.0 — 2026-08-11

- **"리프레시 후 클레임 한 줄 추가" 조치가 더는 필요 없습니다** (ADR 0044): 스캐폴드가 배치·
  기록하는 경로(scripts/harness/ 전부, .githooks/ 훅, CI 워크플로, 시드 파일, .harness-version)를
  게이트 이미터가 **내장 클레임**으로 직접 청구합니다. v0.28.0(.harness-version)과
  v0.29.0(resolve-base.sh) 릴리스 노트의 "기존 스캐폴드 레포 조치 1건"은 **둘 다 취소** —
  이번 리프레시 하나로 파일과 클레임이 같은 순간에 도착하므로 CI가 미클레임으로 실패할 틈이
  없습니다. 레포가 직접 만든 파일의 groups 선언 의무는 그대로입니다(내장 클레임은 정확한 경로만,
  와일드카드 없음; 레포가 선언한 그룹이 항상 우선; 청구 내역은 emitter 출력의
  scaffold-claimed 블록에 전부 표시됩니다).
- **훅 알림이 전부 한글로 나옵니다** (ADR 0045): 세션 시작 알림을 포함한 모든 훅의 사용자
  배너(systemMessage)가 한국어로 통일됐습니다 — 버전 안내만 한글이고 나머지(깃훅 배선 안내,
  스캐폴드 리프레시 안내, 드리프트 배너 등)는 영어였던 혼재가 사라집니다. 상태 마커(UNKNOWN,
  SCHEMA DRIFT 등)·명령어·경로는 그대로 영문 토큰으로 남으므로 복붙·검색은 이전과 동일하게
  동작합니다.

## v0.29.0 — 2026-08-10

- **병합이 만든 상태를 게이트가 봅니다** (ADR 0043, 감사 M-아이템 후속): ① 문서 빌더가 같은
  종류(adr/spec) 안의 **id 중복을 결정론적으로 거부**합니다 — 병렬 브랜치가 각자 '다음 번호'를
  잡으면 양쪽 PR은 초록이고 병합본만 깨지는데, main 푸시 CI가 이제 매 런 재빌드하므로 그 지점에서
  바로 빨갛게 잡힙니다(두 파일명 지목, 한쪽 번호를 옮기면 됩니다). ② 슬라이스 리트로가 **머지
  커밋을 봅니다**: 이전에는 머지 커밋에서 7개 체크 전부가 빈 변경 집합 위를 돌아 조용히
  통과했습니다 — 이제 first-parent 범위(`HEAD^1..HEAD`)로 해석합니다.
- **verify가 낡은 인덱스를 알려줍니다** (ADR 0043): 빌더가 코퍼스 frontmatter 다이제스트를
  `index.json`에 스탬프하고, `/ywr-harness:verify`가 재계산해 어긋나면 `index: STALE` 한 줄로
  재빌드를 권합니다(자문 — 본문만 고친 경우에는 울리지 않습니다). 스탬프는 첫 재빌드
  (`pwsh docs/build.ps1`)에서 자동 생성됩니다.
- **CI 범위 해석이 단일 소스가 됐습니다** (ADR 0043): `scripts/harness/resolve-base.sh`가 세
  번째 벤더링 파일로 배치되어 PR base를 이것 하나로 해석합니다(해석 실패 = 런 실패, 조용한
  통과 없음). **기존 스캐폴드 레포 조치 1건**: 다음 리프레시 후 `.harness.json` groups에
  `scripts/harness/resolve-base\\.sh$` 클레임 한 줄을 추가하세요 — 없으면 CI가 미클레임 파일로
  한 번 크게 실패합니다(`.harness-version` 때와 같은 패턴).

## v0.28.0 — 2026-08-10

- **멀티 라이터 감사 후속 — 강제층이 소리 없이 통과하던 세 상태를 닫았습니다** (ADR 0041):
  ① 게이트 이미터·verify 매퍼가 변경 파일 범위 해석에 실패하면 이제 stdout `scope: FAILED`
  마커와 함께 비영(exit≠0)으로 종료합니다 — 이전에는 빈 출력 + exit 0이라 CI 잡 전체가
  아무것도 검사하지 않고 초록이었습니다. ② main 푸시 CI는 `--all`(전체 트리 감사)로 돕니다:
  각각 초록인 두 PR이 병합으로 만든 조합 상태(예: 한쪽이 그룹 제거, 다른 쪽이 그 그룹의
  파일 추가)를 이제 매 푸시마다 잡습니다. ③ CI에 시크릿 스캔 백스톱이 생겼습니다 —
  `.githooks/pre-push` 스크립트 그대로를 이 런의 범위로 구동하므로(패턴 복제 없음),
  `core.hooksPath` 배선을 안 한 클론의 커밋도 머지 전에 스캔됩니다.
- **스캐폴드 리프레시 안내가 방향을 압니다** (ADR 0042): `/ywr-harness:harness-init`가 성공한
  실행마다 레포 루트에 `.harness-version`(이 레포를 마지막으로 스캐폴드한 플러그인 버전)을
  기록하고, 드리프트 발견 시 안내 훅이 이를 읽어 조언의 방향을 정합니다 — 레포가 내 설치본보다
  새로우면(다른 멤버가 신버전으로 리프레시) "init 금지, 플러그인 업데이트"로 뒤집고, 오래됐으면
  리프레시를 측정된 방향으로 권하며, 같은 버전이면 핸드에딧으로 명명합니다. 스탬프가 없으면
  기존 방향맹 경고가 이제 **사람용 배너에도** 나옵니다(이전에는 모델용 컨텍스트에만 있었습니다).
- **init.ps1이 맹목적 다운그레이드를 거부합니다** (ADR 0042): 레포 스탬프가 실행 중인 플러그인
  버전보다 새로우면 아무것도 쓰지 않고 거부합니다(의도된 롤백은 `-Force`). **기존 스캐폴드
  레포 조치 1건**: 다음 리프레시 후 `.harness.json`의 groups에 `^\\.harness-version$` 클레임
  한 줄을 추가하세요 — 없으면 CI가 미클레임 파일로 한 번 크게 실패합니다(신규 레포의 기본
  선언에는 포함됨).

- 핸드오프 선언이 **작업 라인별 디렉터리 형태**를 지원합니다 (ADR 0040): `.harness.json`의
  `handoff` 값이 `/`로 끝나면 디렉터리 선언입니다 — 작업 라인마다 재개 파일 하나를 두는
  멀티 라이터 레포용 형태로, 파일 이름은 사람이 아니라 작업 라인을 따릅니다. 게이트 이미터가
  `handoff:` 줄에 디렉터리 형태임을 표기하며, 단일 파일 선언은 기존과 동일하게 동작합니다.
- 핸드오프는 **장부가 아니라 재개 지점**입니다: 슬라이스 마감(`/ywr-harness:slice-close`)이
  대체된 Current state 섹션을 `docs/handoff-archive/<같은 파일명>`으로 옮기도록 안내합니다.
  히스토리는 git 체크포인트 커밋에 이미 있으므로, 매 세션 다시 읽는 재개 파일은 얇게
  유지합니다.

## v0.26.0 — 2026-08-07

- 스캐폴드 리프레시 안내 훅이 **낡은 세션(stale session)을 감지합니다** (ADR 0039): 플러그인이
  세션 도중 업데이트되면 실행 중 세션은 `/reload-plugins` 또는 재시작 전까지 이전 버전의 훅을
  계속 실행합니다(플랫폼 동작, 문서 검증). 이 상태에서 기존 안내를 따르면 harness-init 이
  구버전 템플릿을 배치해 **정상 갱신된 레포를 되돌릴 수 있었습니다**(v0.23.2 훅이 v0.25.0
  레포에 리프레시를 권한 실측 사례). 이제 드리프트를 발견하면 훅이 자기 사본이 등록된
  설치본인지 확인하고, 아니면 실행 중 버전과 등록된 버전을 명시하며 `/reload-plugins`(또는
  재시작)를 안내하고 이 세션에서의 harness-init 실행을 금지합니다.
- 확인이 실패하면(레지스트리 없음·파싱 불가·비캐시 사본 등) 기존 안내가 그대로 나옵니다 —
  이 감지는 조언을 고칠 수만 있고 훅을 침묵시키지 않습니다. 등록된 버전이 실행 중 버전과
  같으면(같은 버전의 재등록 — 템플릿이 같아 뒤집힐 것이 없음) 역시 기존 안내를 유지합니다.
  `--plugin-dir` 개발 사본은 기존 방향맹(direction-blind) 주의 문구 그대로입니다.

## v0.25.0 — 2026-08-07

- manifest-gate 에 **스캐폴드 배치본 동일성 검사(검사 클래스 6)** 를 추가했습니다 (ADR 0037
  후속): 플러그인이 레포 안 `plugins/ywr-harness` 에 있는 dogfood 형태에서, 스캐폴드가 배치한
  모든 파일(.githooks/·.github/workflows/·scripts/harness/·docs 툴체인)을 템플릿과 대조해
  드리프트면 빌드를 실패시킵니다. 쌍 목록은 init.ps1 의 배치 맵에서 직접 파싱하고(파싱 실패
  = 실패 — 빈 스윕은 통과가 아닙니다), 비교는 스캐폴드·리프레시 안내와 같은 줄끝 무시
  계약(ADR 0033)입니다.
- 스캐폴드 마커가 없는 GUARDED 파일(post-commit)은 스캐폴드가 거부하듯 검사도 건너뛰고
  이름을 보고합니다. 배치본이 없는 곳(마켓플레이스 캐시 등)에서는 스윕이 스킵을 명시합니다 —
  소비 레포의 멤버 머신 동작에는 변화가 없습니다.

## v0.24.0 — 2026-08-07

- 리뷰 티어에 **파생 사본 면제** 추가 (ADR 0037): `.harness.json`의 새 `review.derived` 키에
  정본→사본 경로 프리픽스 매핑을 선언하면, 현재 바이트가 정본과 동일한 사본은 크기 계상에서
  빠지고 critical 을 강제하지 못합니다 — 정본의 분류가 결정합니다. 드리프트난 사본은 그대로
  계상됩니다(동일성은 이미터가 티어 계산 시점에 직접 검증하며, 심볼릭 링크는 어느 쪽이든
  면제 불가; 상시 게이트가 동일성을 검사하는 쌍은 플러그인 소스↔템플릿뿐 — ADR 0037 후속
  참조). 면제·억제는 티어 라인에 항상 명시됩니다. 순수 전파 diff(사본만 갱신)는 counted 0으로
  small — skip 으로는 절대 내려가지 않습니다. `derived` 를 선언하지 않은 레포는 출력이 바이트
  단위로 동일합니다.
- **선언 파일 criticality 를 변경 키에서 계산** (ADR 0038): `.harness.json` 이 diff 에 있으면
  변경된 최상위 키를 계산해(주석 키 재귀 제거), {docs, handoff, artifacts}·주석에 국한된
  변경은 critical 을 강제하지 않습니다. 그 외 키(groups, verify, review, retro)는 선언 여부와
  무관하게 모든 레포에서 critical — 선언이 스스로를 옵트아웃할 수 없습니다. 기준을 알 수 없는
  경우(새로 추가된 선언, 파싱 불가)도 critical 입니다.
- 스캐폴드 템플릿 `harness.json` 에 `derived` 키를 문서화했습니다(기본 빈 목록 = 면제 비활성).
  (참고: CHANGELOG.md 를 docs_only 로 분류하는 변경도 이 슬라이스에 있었으나, 적대 리뷰가
  버전 안내 훅(ADR 0030)이 이 파일의 불릿을 모든 멤버 세션 시작에 렌더링함을 확인해 같은
  슬라이스에서 철회했습니다 — 릴리스 노트는 세션에 도달하는 산문이므로 리뷰 면제가 아닙니다.)

## v0.23.2 — 2026-08-06

- 게이트 스크립트의 **저심각도 하드닝 3건** (2026-07-29 리뷰에서 큐잉된 후속 조치): ① 토큰
  검증 정규식의 앵커를 `$` → `\Z`로 교체 — 파이썬의 `$`는 끝 개행 앞에서도 매칭되므로, 호출부
  전처리에 기대지 않고 검증기 자체가 개행 꼬리를 거부합니다. ② 명령 조립 단계의 cwd 2차
  거부가 이제 경고를 남깁니다(이전엔 조용히 `cd`만 생략). ③ 스크립트 게이트 선언의 모르는
  키(`"args"` 등)는 경고와 함께 무시되고, `"files"`에 불리언 아닌 값(문자열 `"false"` 등)이
  오면 참으로 강제되지 않고 경고 후 false(전체 프로그램)로 처리됩니다.
- 게이트 이미터의 **문구 정확화 2건**: 게이트가 하나도 안 나왔을 때의 안내가 "선언된 게이트가
  전부 거부/스킵됨" 원인도 명시합니다. 인자로 못 넘기는 파일명이 있어도 그룹에 전체 프로그램
  게이트가 있으면 "UNGATED"라고 과장하지 않고 파일 단위 커버리지 손실만 정확히 보고합니다.
- 동작 변경은 ③의 비불리언 `files` 처리 하나를 제외하면 경고·문구 추가뿐입니다. 예외를 정확히
  말하면: `"files"`에 문자열 `"true"` 같은 참-강제 값을 선언했던 레포는 이전엔 (우연히) 파일
  스코프 게이트로 동작했고, 이번부터는 경고와 함께 전체 프로그램 게이트로 동작합니다 — 불리언
  `true`/`false`를 선언한 레포는 영향 없습니다.

## v0.23.1 — 2026-08-06

- 스캐폴드 배치의 **줄끝 결함 수정** (ADR 0036): 플러그인 캐시가 CRLF로 체크아웃되는 머신
  (git `core.autocrlf`)에서 `/ywr-harness:harness-init` 가 템플릿의 CRLF 바이트를 그대로
  복사했습니다 — eol=lf 레포에서는 재실행 때마다 가짜 수정 파일이 보이고, POSIX 클론에서는
  배치된 git 훅이 `/bin/sh^M: bad interpreter` 로 죽는 결함. 이제 배치가 줄끝을 소유합니다:
  쓸 때 CRLF 는 LF 로 접히고(쌍만 — 단독 0x0D 는 내용으로 보존), 줄끝만 다른 파일은 변경으로
  세지 않습니다(리프레시 안내 훅과 같은 비교 계약, ADR 0033). 동작 변경은 이것뿐이며, 시드
  보존·거부 규칙은 그대로입니다.

## v0.23.0 — 2026-08-06

- 리뷰 티어의 **크기 측정이 클래스 가중**으로 바뀌었습니다 (ADR 0035): `.harness.json`에
  `docs_only`로 선언된 문서 표면(실행 의미가 없다고 이미 선언된 파일)은 small/full 크기
  임계값 계산에서 제외됩니다. 작은 기능 변경에 ADR·재생성 인덱스·README가 따라붙는 흔한
  슬라이스가 문서 파일 수 때문에 full 리뷰 비용을 내던 문제의 수정입니다.
- **리뷰 범위는 그대로입니다** — 제외는 측정에만 적용되고, 제외가 발생하면 티어 사유
  줄에 몇 개가 제외됐는지 명시됩니다(조용한 축소 없음). critical 표면 우선, 전부 문서면
  skip, `docs_only` 미선언 레포의 출력은 바이트 단위로 동일합니다.

## v0.22.0 — 2026-08-06

- **지금 당장 업데이트** 명령이 생겼습니다: `/ywr-harness:update` 는 백그라운드 자동업데이트
  (다음 세션 시작 시 반영)를 기다리는 대신, 마켓플레이스 카탈로그 갱신과 플러그인 업데이트를
  즉시 실행하고 디스크 버전 변화(이전 → 새 버전)를 보고합니다.
- 자동화할 수 없는 두 단계는 매번 순서대로 안내합니다: ① `/reload-plugins` 입력(또는 재시작)
  — 실행 중인 세션은 시작 시 로드한 버전을 유지하므로 필수, ② 다음 세션 시작 시 스캐폴드
  안내(ADR 0033)가 드리프트를 알릴 **때만** `/ywr-harness:harness-init` 를 레포당 1회 실행.
  스캐폴드 리프레시를 자동 실행하지는 않습니다 — 설치된 플러그인보다 의도적으로 새로운 작업
  트리를 구버전으로 되돌릴 수 있기 때문입니다 (ADR 0034).

## v0.21.0 — 2026-08-06

- 세션 시작 시 **스캐폴드 리프레시 안내**가 생겼습니다: `/ywr-harness:harness-init`로 깔린
  툴체인(벤더 게이트 스크립트 · CI 워크플로 · git 훅 · docs 빌더)이 설치된 플러그인의
  템플릿과 다르면 — 즉 플러그인 업데이트의 개선이 이 레포에 아직 반영되지 않았거나 배치
  파일이 손으로 수정됐으면 — 다른 파일 수와 목록, 리프레시 명령 한 줄을 제안합니다
  (ADR 0033). 리프레시는 **레포당 1회**(한 사람이 실행해 커밋)면 됩니다. **제안만** 하며
  아무것도 바꾸지 않고, 차이가 없으면 침묵합니다.
- 시드(`CLAUDE.md` · `.harness.json` 등 레포 소유 파일)와 마커 없는 `post-commit`(레포
  자체 훅)은 비교하지 않습니다 — 스캐폴드가 덮어쓰지 않는 파일은 안내 대상도 아닙니다.
  줄끝(CRLF/LF)만 다른 파일은 같은 것으로 취급합니다. 자동 훅은 6종이 됩니다.

## v0.20.0 — 2026-08-05

- 레포가 발행한 claude.ai 아티팩트를 `.harness.json`의 `artifacts` 섹션에 선언하면, 하네스
  게이트가 **README에 그 링크가 있는지**와 **선언된 제목이 레포 이름으로 시작하는지**
  (`<레포명> · <용도>` 규칙)를 검사하고, 위반 시 CI가 실패합니다 (ADR 0032).
- 선언하지 않은 레포는 이전과 동일하게 동작합니다 — 검사가 꺼져 있다는 사실만 게이트
  출력에 한 줄로 보고됩니다.
- 선언된 `url`/`title` 에 제어문자(줄바꿈 등)가 있으면 위반으로 보고합니다. 나아가 게이트
  emitter 와 `/ywr-harness:verify` 매퍼의 **모든 출력 줄이 한 곳을 통과**하도록 바뀌어, `.harness.json` 의
  값·그룹 이름·파일명 등 레포가 제공한 어떤 텍스트도 출력 줄을 새로 만들 수 없습니다 — 그런
  줄이 만들어지면 CI 와 pre-commit 이 그것을 게이트 명령으로 착각해 실행할 수 있었습니다.
  경고(stderr) 줄도 같은 처리를 받습니다: 커밋 중 터미널에 ESC 시퀀스가 그대로 나가면 훅의
  출력을 덮어쓸 수 있기 때문입니다.

## v0.19.0 — 2026-08-05

- 플러그인이 이 머신에서 **처음 적용되는 세션**에도 한 줄 안내가 뜹니다: 적용된 버전과
  릴리스 노트 링크만(불릿 없음), 머신당 1회. 신규 설치자, 그리고 v0.18을 건너뛰고 바로
  이후 버전으로 온 경우가 여기에 해당합니다 (ADR 0031).
- 기존의 "vA → vB 업데이트됨" 안내는 그대로입니다 — 이전 버전이 기록된 머신에서만
  나옵니다.

## v0.18.0 — 2026-08-05

- 버전 업데이트 안내가 생겼습니다: 새 버전이 처음 로드된 세션 시작 시, 무엇이 바뀌었는지
  요약(이 파일의 해당 항목)과 릴리스 노트 탭 링크를 **버전당 한 번** 보여 줍니다. 이미 본
  버전에서는 침묵합니다.
- 마지막으로 안내한 버전을 `~/.claude/ywr-harness/announced-version` 한 줄 파일에
  기록합니다 — 플러그인이 사용자 스코프에 쓰는 유일한 파일입니다.
- 릴리스 노트 정본(이 파일)이 플러그인과 함께 배포되고, 온보딩 가이드에 릴리스 노트 탭이
  추가됐습니다. 자동 훅은 5종이 됩니다.

## v0.17.0 — 2026-08-05

- 세션 시작 시 git hooks 미배선 안내가 생겼습니다: 레포에 `.githooks/`가 있는데 이 클론의
  `core.hooksPath`가 설정돼 있지 않으면, 배선 한 줄 명령(`git config core.hooksPath
  .githooks`)을 제안합니다. **제안만** 하며 아무것도 자동으로 바꾸지 않습니다 (ADR 0029).
- 배선된 클론, `.githooks/`가 없는 레포, 다른 훅 경로를 쓰는 레포에서는 침묵합니다.

## v0.16.1 — 2026-08-04

- 배포 레포 README에 온보딩 가이드 링크가 실렸습니다 (claude.ai Team 좌석 로그인 필요 —
  README에 명시). 별도 공지 대신 이 링크가 상시 안내 경로입니다.

## v0.16.0 — 2026-08-04

- 리뷰 리추얼이 정리됐습니다: 적대 코드리뷰는 **슬라이스당 1회**이며, 확정 지적을 고친 수정
  diff는 두 번째 전체 리뷰가 아니라 게이트 재실행 + 지적별 수정 확인으로 마감합니다
  (ADR 0028). `ywr-harness:slice-close` 스킬과 리뷰 워크플로 안내문에 반영됐습니다.

## v0.15.0 — 2026-07-31

- 상태바 끝에 **이 머신에 설치된 플러그인 버전**(`ywr-harness vX.Y.Z`)이 표시됩니다. 자동
  업데이트로 디스크가 먼저 새 버전이 되므로, 세션과 디스크의 버전 차이가 곧 "재시작하면
  적용" 신호입니다 (ADR 0027).
- 기존에 상태바를 설치한 머신은 `install.ps1`을 다시 실행해야 새 세그먼트가 나옵니다.

## v0.14.0 — 2026-07-31

- `ywr-harness:verify`가 전용 verifier 에이전트로 실행됩니다 (sonnet · effort medium —
  A/B 측정으로 결정, ADR 0025). 절차 수행 검증 비용이 내려갑니다.
- 같은 날 관리 설정으로 마켓플레이스 **자동 업데이트가 켜졌습니다** (ADR 0026): 이후 버전은
  다음 세션 시작 후 몇 분 안에 자동 설치되며, `claude plugin update`를 직접 돌릴 필요가
  없습니다. 실행 중인 세션은 재시작해야 새 버전을 씁니다.

## v0.13.0 — 2026-07-29

- `.harness.json`에 **스크립트 게이트**를 선언할 수 있습니다: 레포 자체 게이트 스크립트를
  클로즈드-셋 러너 + 검증된 레포 경로로 선언하면 CI와 pre-commit이 실행합니다 (ADR 0024).
  자유 명령 문자열은 계속 거부됩니다.
- 게이트 명령 조합 경로의 인젝션 방어가 강화됐습니다 (적대적 파일명·`cwd`·빈 인자 목록 모두
  한 초크포인트에서 검증).

## v0.12.3 — 2026-07-28

- Windows 설치본에서 워크플로 이름 호출(`ywr-harness:adversarial-review`)이 죽던 CRLF 결함이
  고쳐졌습니다 — 배포본에 `.gitattributes`가 실려 체크아웃이 LF로 고정됩니다.

## v0.12.2 — 2026-07-28

- `/ywr-harness:harness-init`가 기존 ADR 로그를 제대로 인식합니다: 다양한 이름·위치의 기존
  결정 기록이 있는 레포에 두 번째 `0001`을 심던 결함이 고쳐졌습니다. 이미 잘못 심어졌다면
  중복 `0001-adopt-docs-as-code.md`를 지우고 스캐폴드를 다시 실행하세요.

## v0.12.1 — 2026-07-28

- 플러그인 README 전제조건이 정비됐습니다 (python 3.9+ 명시 — 게이트 스크립트가 씁니다).

## v0.12.0 — 2026-07-27

- 최초 배포판입니다. 마켓플레이스 `ywrlabs`는 관리 설정으로 자동 등록되며, 설치는 각자
  `claude plugin install ywr-harness@ywrlabs` — 자격증명 없이 익명 HTTPS로 받아집니다
  (ADR 0023). 설치는 강제되지 않습니다.
- 구성: 자동 훅 3종(설정 변경 감사 · 디렉터리 추가 가드 · 서브에이전트 텔레메트리), 적대
  코드리뷰 워크플로, `/ywr-harness:harness-init` · `/ywr-harness:verify` ·
  `/ywr-harness:slice-close` 스킬, 상태바 설치기, worker/mech 에이전트.
